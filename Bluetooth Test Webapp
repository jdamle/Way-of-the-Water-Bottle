/* App.tsx */

import * as React from "react";
import {TextInput, Text, StyleSheet} from "react-native";
//import { useSmartLight} from "./hooks/useSmart";
import {connect, disconnect,read, write, setTemp} from "./hooks/useEdit";
//var testing = document.getElementById("message");
//var test = {id="message"};

<script type='text/javascript' src='./hooks/useEdit.js'></script>

//let isConnected = false;
export const App = () => {
  

  //const { connect, toggle, isConnected } = useSmartLight();
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
            //sends global input back to useEdit
            
    >       <button onClick={disconnect}>disconnect</button>
            <TextInput  
            id="temp"/> 
            <span id="message-recieve" > Waiting for message...</span>
            <button onClick={setTemp}>Send</button>
            <button onClick={connect}>Connect</button>
            <Text style={styles.subtext}>Current Temperature: 40 degrees</Text>
 
      
    </div> 
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    //paddingTop: Constants.statusBarHeight,
    backgroundColor: '#d4c1f0',
    padding: 0,
  },
  paragraph: {
    margin: 24,
    fontSize: 20,
    fontWeight: 'bold',
    textAlign: 'center',
    fontFamily: 'Georgia',
    color: '#2b007a',
  },
  container1: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#8c6bc3',
    color: '#2b007a',
  },
  subtext: {
    margin: 10,
    fontSize: 15,
    fontFamily: 'Georgia-Italic',
    textAlign: 'left',
    color: '#2b007a',
  },
});

export default App;
